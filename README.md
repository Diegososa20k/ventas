npm run dev para correr el backend
npm start para correr electron
npm run build para construir el programa o .exe

de no funcionar, hay que hacerlo directo desde cmd abierto como administrador

buscas la direccion de la careta desde el cmd, y usar el mismo comando
